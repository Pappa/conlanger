Type: task
Status: ready-for-agent
Blocked by: None

# Correction pass: flatten nested sets in env / exception

Target cluster: `nested_brackets` — nested `{…}` in **env** or **exception** only (~16 inventory rows at 2026-08-12 baseline).

Spawned from [spike 67](67-spike-nested-sets.md). Findings: [nested-sets-inventory.md](../research/nested-sets-inventory.md) §4.2–4.4.

## Problem

Index encodes env/exception alternations with nested braces, e.g.:

- `_ə{(C){p,kʷ},m,w}` (`index_diachronica_original.html:1903`)
- `_{s,({m,j,w})V}` (`index_diachronica_original.html:5509`, `:5510`)
- `{{C[-fr,+bk,-hi,-lo],K}ʷ,w}_` (`index_diachronica_original.html:11552`)

ASCA 0.10.2 rejects nested `{}` of the same bracket type at lex time (`NestedBrackets`).

## What to build

1. Compile-time flatten for env/exception fields (mirror ticket [23](23-correction-pass-labialized-class-letters.md) set flattening).
2. Expand common shapes: `({m,j,w})V` → `{mV,jV,wV}`; one-level union flatten `{a,{b,c}}` → `{a,b,c}` when semantically safe.
3. TDD on representative inventory lines; full regen + metrics.

## Policy

- ADR-0010: no valid-but-inaccurate rewrites; preserve `raw`.
- YAML `env` / `exception` may change at compile emission only, or via parse-time normalizer if already used for env — follow [asca-compile-transform-order.md](../research/asca-compile-transform-order.md).

## Acceptance criteria

- [ ] Handler + tests on §4.2–4.4 examples from research
- [ ] Full inventory re-baseline; `nested_brackets` count reduced; metrics in **Answer**
- [ ] Residual rows documented (manual_mappings / skip)

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement  
**Summary:** Compile-time flatten of nested `{…}` in **env** and **exception** fields so ASCA 0.10.2 no longer hits `NestedBrackets` on context sets.

**Current behavior:** Env/exception strings pass through the compile pipeline (`compile_asca_rule_string` in `src/conlanger/tools/compile/asca/pipeline.py`) with parenthetical/set expansion in `parenthetical.py` / `expand_meta_notation`, but true nested braces in context fields still fail validation (~14 env + ~7 exception inventory rows per [nested-sets-inventory.md](../research/nested-sets-inventory.md) §4.2–4.4).

**Desired behavior:**
- Add a compile step (or extend `expand_meta_notation` / parenthetical helpers) that flattens **env** and **exception** only — not stages I/O (that is ticket [70](70-correction-pass-flatten-nested-io-sets.md), blocked by grill [71](71-grill-paren-and-parallel-set-notation.md)).
- Handle shapes from research §4.2–4.4:
  - One-level union flatten: `{a,{b,c}}` → `{a,b,c}` when members are comma-bounded alternatives.
  - Parenthetical-in-set expansion: `({m,j,w})V` → `{mV,jV,wV}`; `_ə{(C){p,kʷ},m,w}` → flattened env members.
  - Feature-matrix nests like `{{C[-fr,+bk,-hi,-lo],K}ʷ,w}_` → single-level set per ticket [23](23-correction-pass-labialized-class-letters.md) precedent.
- Preserve **`raw`**; YAML may change only at compile emission (ADR-0010). Follow transform order in [asca-compile-transform-order.md](../research/asca-compile-transform-order.md) — place after group mappings / before or within `expand_meta_notation` as appropriate for env-only application.

**Key interfaces:**
- `compile_asca_rule_string` / `ASCA_COMPILE_STEP_NAMES` — wire new step or hook env/exception field walkers in the compile path that joins rule strings.
- `src/conlanger/tools/compile/asca/parenthetical.py` — existing `_expand_sets` / `_expand_set_member` may be reusable; do not conflate with I/O optional-prefix parallel columns (§4.5 — grill 71 scope).
- Tests: `tests/conlanger/tools/compile/asca/` — TDD on HTML `source` lines from §4.2–4.4 table.

**Acceptance criteria:** (same as ticket body)
- [ ] Handler + tests on §4.2–4.4 examples from research
- [ ] Full inventory re-baseline; `nested_brackets` count reduced; metrics in **Answer**
- [ ] Residual rows documented (`manual_mappings` / skip)

**Out of scope:** Stages I/O nested sets ([70](70-correction-pass-flatten-nested-io-sets.md)); `(h)ə{p,b}` optional-prefix parallel columns ([71](71-grill-paren-and-parallel-set-notation.md)); chain malformation / unbalanced braces (manual_mappings or skip).

**Blocker cleared:** Spike [67](67-spike-nested-sets.md) resolved; bucket taxonomy and examples in research doc.
