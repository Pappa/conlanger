Type: grilling
Status: resolved
Blocked by:

# Grill: structured compile intermediate representation on SoundChangeRule

Owner (2026-08-21, after [ticket 92](92-grill-pydantic-compile-refactor.md)): hold **input** / **output** (and env/exception where it fits) as **collections** inside `SoundChangeRule` — sequences for **parallel tokens**, ordered collections for `{…}` **field tokens** — so **optional outputs** and parallel-∅ **alternatives** are structural, not string-gated. **Do not fold into [ticket 93](93-pydantic-compile-refactor.md).** Run this grill **after [99](99-compiled-fields-join-at-render.md)** lands compiled field strings joined at render. **No code in this grill.**

Run `/grill-with-docs`.

## Question

What is the compile-time internal representation of a `SoundChangeRule`’s input, output, env, and exception — Index strings vs ordered **field tokens** vs a richer tree — so alternatives, **parallel tokens**, and context fields can be detected and transformed without regex on blobs? YAML **stages** stay opaque ([ADR-0002](../../../docs/adr/0002-applier-neutral-yaml-rule-index.md), **optional outputs** in `CONTEXT.md`). Glossary: **Compile field**, **Field token**, **Compile-field intermediate representation**, **Parallel tokens** in `CONTEXT.md`.

## Settled (owner confirmed 2026-08-31)

| Id | Decision |
|----|----------|
| Q1 | **Phased v1:** ordered tuple of **field tokens** per compile field — singleton or ordered set. Richer tree v2; ticket 71 on string layer until then. |
| Q1b | Field tokens per compile field; zip input/output at rule level. |
| Q2 | Parse inside `compile_rule`. **`RuleInput`**, **`RuleOutput`**, **`RuleEnv`** (exception reuses **`RuleEnv`**) — Index-raw + compiled ASCA on each object. |
| Q3 | Same field tier for env/exception. |
| Q4 | Defer ticket 71 from v1. |
| Q6 | Expand `(ː)` to ordered alternation; **optional-length node** before suffix `length_marks`. |
| Q5 | Alternative detection on **field-token structure** (optional outputs + parallel-∅). |
| Q7 | **Hybrid v1 transforms** — walk tokens for structural paths; legacy stringify→reparse until migrated. |
| Q8 | **Field render → join** — compiled string per field object; `__str__` joins four. |
| Q9 | **Structure + golden** tests. |
| Terminology | Top-level unit = **field token**; **Parallel tokens** for multi-token alignment. |

## Outcomes

- [x] Recorded intermediate representation (field tokens; not Python `set`).
- [x] Ticket 71 deferred from v1.
- [x] Ticket 104 policy locked (expand `(ː)` via optional-length node).
- [x] ADR [0015](../../../docs/adr/0015-compile-field-intermediate-representation.md).
- [x] Implementation follow-on: [105](105-implement-compile-field-intermediate-representation.md).

## Answer

Owner confirmed full design (2026-08-31). ADR [0015](../../../docs/adr/0015-compile-field-intermediate-representation.md). Implementation: [105 — Implement compile-field intermediate representation](105-implement-compile-field-intermediate-representation.md) (**resolved**). Unblocked [104](104-correction-pass-parenthesized-optional-length-marker.md) optional-length policy.

## Session summaries

**2026-08-30 — Round 1:** Compile-field pydantic wrappers with raw + compiled; ticket 71 deferred; glossary started.

**2026-08-31 — Round 2:** Phased v1 field tokens; Q6 expand `(ː)`; renamed glossary to **field token** / **parallel tokens**.

**2026-08-31 — Round 3:** Q5–Q9 settled per recommendations; grill closed.

## Comments

> *This was generated by AI during triage.*

**2026-08-29:** Blocked on [99](99-compiled-fields-join-at-render.md).

**2026-08-30:** [99](99-compiled-fields-join-at-render.md) resolved; Round 1 partial.

**2026-08-31:** Rounds 2–3; **resolved**; ADR 0015 + ticket 105 filed.
