Type: task
Status: resolved
Blocked by: None

# Sporadic sampling (apply vs skip) on shared instance RNG

Spawned from [grill 61](61-grill-optional-outputs.md). Ticket 19 already strips uncertainty glosses and sets `sporadic: true` (~195 rules); apply/skip at render/apply time was left aspirational.

## Problem

**Sporadic** means the rule may or may not apply (distinct from **optional outputs**). Grill 61 deferred behavior but required RNG plumbing on [66](66-implement-optional-outputs-alt-idx.md) to stay reusable (`random.Random`, not global seed).

## Triage decisions (settled 2026-09-01)

| Decision | Outcome |
|----------|---------|
| Default apply probability | **0.5** — uniform apply/skip on render path |
| Per-rule rate in index | **No** — boolean `sporadic: true` only; SoT stays thin |
| Inventory | **Always apply** — no skip branch, no `sporadic_idx` column |
| Unit tests | **Always apply** except **one** test confirming skip via injected `rng`/`seed` |
| Draw order (sporadic + optional outputs) | **Sporadic gate first**, then optional-output alternative pick (same instance `Random`, sequential draws) |
| Skip at render | **`#\t` commented line** (reuse `skip_prefix` pattern from `status: skipped`) — do not omit from series |

## What to build

1. Use the same instance-`Random` story as optional outputs (from 66).
2. When `sporadic` is true on the render path, sample apply (probability **0.5**) vs skip; skip renders as `#\t` + compiled fields.
3. **Do not** fold sporadic into `alternatives` (grill Q9: separate gate).
4. **Draw order:** sporadic apply/skip **before** optional-output alternative selection when both apply.
5. **Inventory:** always apply — sporadic rules validate as if applied; no skip branch rows.
6. **Tests:** always apply in unit and inventory tests; **one** unit test confirms skip behavior via injected `rng` / `seed`.
7. Add **Sporadic** glossary entry to `CONTEXT.md` (distinct from **Optional outputs**).

## Acceptance criteria

- [x] Triage decisions recorded (rate, inventory, draw order, skip render)
- [x] Inventory always applies sporadic rules (no skip branch)
- [x] Sporadic apply/skip uses instance `Random` from 66’s plumbing (render path)
- [x] `alternatives` remains optional-outputs-only
- [x] One unit test confirms sporadic skip via injected `rng`; all other tests always apply
- [x] Unit + inventory behavior documented in **Answer**

## Answer

Implemented 2026-09-01.

### `SoundChangeRule` (`src/conlanger/tools/rules.py`)
- Added `sporadic: bool`, frozen `sporadic_skipped: bool`, and render-only `sample_sporadic` gate (default `False` on direct construction; `DiachronicSeries` sets `True` for `.rsca` emission).
- Sporadic apply/skip draws `rng.random() >= 0.5` → skip **before** optional-output `randrange` when both apply; skip compiles fields without alternative pick and renders `#\t` + compiled join (not Index `raw`).
- Instance `Random` only (`seed` / caller `rng`); shared across chained steps in one `DiachronicSeries`.

### Inventory (`src/conlanger/tools/index_inventory.py`)
- `_resolve_inventory_targets` builds `DiachronicSeries(..., sample_sporadic=False)` so sporadic rules always validate as applied; no `sporadic_idx` column or skip-branch rows.

### Tests (`tests/conlanger/tools/test_sporadic_sampling.py`)
- One skip test (`test_sporadic_skip_renders_commented_compiled_fields`) with injected rng; all other sporadic tests force apply via `sample_sporadic=False` or apply-path rng.
- Draw-order test confirms sporadic gate before optional-output `randrange`.

### `CONTEXT.md`
- Added **Sporadic** glossary entry; updated **Optional outputs** cross-reference.

Full gate green: `uv run pytest` → **1318 passed**, coverage ≥95%; ruff clean.

## References

- [Grill 61](61-grill-optional-outputs.md)
- [Ticket 19](19-correction-pass-sporadic-qualifier.md) — flag only
- [Implement optional outputs](66-implement-optional-outputs-alt-idx.md) — RNG prerequisite
- `CONTEXT.md` — **Optional outputs** vs sporadic

## Comments

> *This was generated by AI during triage.*

**2026-08-29:** Prerequisite [66](66-implement-optional-outputs-alt-idx.md) is `resolved`. This ticket stays `needs-triage` (rate / inventory / draw-order still open).

> *This was generated by AI during triage.*

**2026-09-01:** Owner settled inventory + test policy:
- **Inventory** always applies sporadic rules (never skip, no branch enumeration).
- **Unit tests** always apply sporadic rules except **one** test that confirms skip works via injected `rng`/`seed`.

> *This was generated by AI during triage.*

**2026-09-01:** Owner confirmed remaining render-path defaults → **`ready-for-agent`**:
- Apply probability **0.5**; no per-rule rate in index.
- Draw order: sporadic gate **before** optional-output pick.
- Skip renders as **`#\t`** commented line (not omitted).

## Agent Brief

**Category:** enhancement
**Summary:** Sample apply vs skip for `sporadic` rules at render time using instance `Random`, with inventory/tests always applying.

**Current behavior:**
Index rules may carry `sporadic: true` (parse-time flag from ticket 19; ~350 rules). The flag propagates through chain expansion but `SoundChangeRule` ignores it — no apply/skip sampling, no commented skip render. Optional outputs (ticket 66) already use instance `random.Random` for uniform alternative picks.

**Desired behavior:**
On the **render path** (`DiachronicSeries` / `SoundChangeRule` string emission), when `sporadic` is true:
1. Draw apply vs skip with probability **0.5** using the rule's instance `Random` (optional `seed` / caller-supplied `rng`; never process-global `random.seed`).
2. **Apply:** emit the rule normally (then run optional-output alternative pick if applicable — sporadic draw is **first**, alternative pick **second**, same `Random` instance).
3. **Skip:** emit `#\t` + compiled fields (same prefix pattern as `status: skipped`), frozen at instantiation — no re-sample on `str()`.

**Inventory and tests:**
- Inventory **always applies** sporadic rules (never samples skip; no new branch column).
- Unit tests **always apply** sporadic rules except **one** test that injects `rng`/`seed` to assert skip renders as `#\t…`.

**Key interfaces:**
- `SoundChangeRule`: accept `sporadic: bool`; gate apply/skip before alternative resolution; expose frozen skip state for render.
- `DiachronicSeries`: pass `sporadic` and shared `rng` through chain-expanded steps.
- `expand_chained_index_rule`: already propagates `sporadic` — ensure compiled instances receive it.
- Inventory validation path: force apply (bypass sporadic sampling or set `sporadic=False` for validation targets).
- `CONTEXT.md`: add **Sporadic** glossary term (apply-or-skip uncertainty; distinct from optional outputs).

**Acceptance criteria:**
- [ ] Render path: `sporadic: true` → 50% apply / 50% skip via instance `Random`; skip → `#\t` + compiled value
- [ ] Draw order: sporadic gate before optional-output `alternatives` pick when both present
- [ ] `alternatives` remains optional-outputs-only (not used for sporadic)
- [ ] Inventory always validates sporadic rules as applied (no skip branch)
- [ ] One unit test proves skip via injected `rng`; all other sporadic tests always apply
- [ ] `CONTEXT.md` documents **Sporadic** vs **Optional outputs**
- [ ] Full gate green (`uv run pytest`, ruff, per-file coverage)

**Out of scope:**
- Per-rule apply-rate overrides in index YAML
- `sporadic_idx` inventory column or skip-branch rows
- Folding sporadic into `alternatives`
- Changing parse-time `sporadic` detection (ticket 19)
