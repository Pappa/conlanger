Type: task
Status: wontfix
Blocked by:

# Correction pass: Australasian `*X` wildcard insertion / correspondence

Target cluster: `syntax_other` — **`*R`/`*L`/`*j`/`*D`/`*Z` in insertion inputs and correspondence sets** — **23** rules in ≤3-fail sections (**6** mono-class sections would complete if cleared). Full-index count TBD at claim time ([inventory summary](../inventory/asca-rule-inventory-summary.md)).

Spawned from [64 syntax_other near-miss spike](../issues/64-spike-syntax-other-near-miss-sections.md) (2026-08-19).

## Context

Two related subclusters in Austronesian / Vanuatu sections:

| Subcluster | rules | example |
|------------|------:|---------|
| Insertion input `*X` | 10 | `*R > ∅`, `*L > r`, `*j > ɡ / _#` |
| Wildcard in correspondence set | 13 | `{z,*Z,*D,*j} > d`, `{s,*R} > r / _#` |

ASCA insertion rules require input `*` or `∅` only; `*R` is a correspondence-series placeholder. Overlaps [series mappings policy](../series-mappings-coverage-backlog.md) — prefer `series_expansions` / collective expand over ad-hoc IPA hacks when `*X` is a known series index.

## What to build

1. Inventory which `*X` tokens map to known series (R, L, D, Z, j, …) vs one-off.
2. Parse/compile rewrite: expand series to concrete sets, or rewrite `*X` insertion → bare `*` + env/structure when faithful.
3. Full inventory re-run; record before/after for both subclusters.
4. Coordinate with [65 series coverage](65-series-mappings-coverage-pass.md) — do not duplicate series CSV work.

## Policy

- Series-first when `*X` is a documented correspondence index.
- Compile/parse fix; `raw` unchanged.

## Acceptance criteria

- [ ] `*X` token inventory with series vs ad-hoc classification
- [ ] Transform implemented for dominant tokens
- [ ] Full inventory re-run; before/after in **Answer**
- [ ] Fixtures updated where validation outcomes change

## References

- [Spike: syntax_other near-miss sections](64-spike-syntax-other-near-miss-sections.md)
- [Series mappings coverage pass](65-series-mappings-coverage-pass.md)
- [Correction pass template](13-correction-pass-template.md)
- [Implement parser_config section_mappings](97-implement-parser-config-section-mappings.md)

## Answer

**Wontfix — already implemented** (triage 2026-08-31). The planned compile/parse correction pass is superseded by parse-time **`section_mappings`** ([ticket 97](97-implement-parser-config-section-mappings.md)) plus config expansion in [`config/parser/parser_config.yml`](../../../config/parser/parser_config.yml) (section **`10`**: `*D`, `*R`, `*T`, `*Z`, `*j`, `*r`, `*c` → class letters). Applied via `apply_section_mappings()` in [`src/conlanger/utils/mappings.py`](../../../src/conlanger/utils/mappings.py) during ingest.

**Inventory check (current `asca-rule-inventory-error.csv`):** no remaining failures matching ticket 83 subclusters (`insertion_wildcard_input`, `wildcard_in_correspondence_set`). §10.x `*R`/`*D`/`*Z`/`*j` rules are **ok** in success CSV. Residual `*X` errors are out of scope:

| Rule | Section | Why not 83 |
|------|---------|------------|
| `Proto-Ongan-ɟ-ɡ-*N-*R` | 10.5 | `*N` token; `prose_or_expected_arrow` |
| `Proto-Tsouic-*C,d-j-*R` | 10.8 | `*C` not in section mappings |
| `Basque-*L` | 44.2 | `*L` mapped via manual_mappings; fails on editorial `(or *lh` prose |

`*L` outside §10 handled by [`manual_mappings.yml`](../../../config/parser/manual_mappings.yml) rows (e.g. Erromango Sye/Ura, Paiwan, Basque).

## Comments

> *This was generated by AI during triage.*

**Category:** enhancement (correction pass)  
**State:** `wontfix` — already implemented via [97](97-implement-parser-config-section-mappings.md) `section_mappings`, not a separate compile transform.

Ticket 83 assumed a dedicated correction pass rewriting `*X` → class letters or bare `*`. That policy landed at parse time instead — the approach ticket 83's own "series-first" policy preferred. No `.out-of-scope/` entry (built, not rejected).
